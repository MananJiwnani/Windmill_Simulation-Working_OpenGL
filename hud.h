#ifndef HUD_H
#define HUD_H

/* Render the 2D HUD overlay dashboard */
void render_hud(int fbW, int fbH);

/* Render 2D wind direction arrow compass on HUD */
void render_wind_direction_arrow(int fbW, int fbH);

#endif // HUD_H
